\DocumentMetadata{}
% \documentclass[acmsmall]{acmart}
\documentclass[sigconf]{acmart}
% \documentclass{llncs}
\newif\ifacm
\newif\iflncs
\IfClassLoadedTF{acmart}{\acmtrue}{}
\IfClassLoadedTF{llncs}{\lncstrue}{}
\usepackage[T1]{fontenc}
\usepackage{pifont}
% \usepackage{microtype}
% \usepackage{amsmath}
% \usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
% \usepackage{tikz-cd}
% \usepackage{tabularx}
\usepackage[shortlabels]{enumitem}
\usepackage{xspace}
\usepackage{proof}
\usepackage{minted}
\usepackage{listings}
\usepackage{color}
\usepackage[svgnames]{xcolor}
% \usepackage{amssymb}
% \usepackage{syntax}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{bussproofs}
\usepackage{nameref}
\usepackage{hyperref}
% \usepackage[nameinlink,sort]{cleveref}
\usepackage{zref}
\usepackage{zref-clever}
\renewcommand\UrlFont{\color{blue}\rmfamily}

% \newcommand{\backslash}{\char`\\}
\setminted{autogobble,breaklines,fontsize=\footnotesize}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  columns=fullflexible,
  keepspaces=true,
  breaklines=true,
  breakatwhitespace=true,
  frame=single,
  framerule=0pt,
  showstringspaces=false,
}
\lstdefinestyle{bash}{
  prebreak=\mbox{\textbackslash},
  % postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
  language=bash,
  alsoletter={-},
  stringstyle=\color{codepurple},
  commentstyle=\color{codegreen},
  keywordstyle=\color{magenta},
  % morekeywords={sudo, chmod, chown, systemctl} % Add extra CLI tools here
}
\usepackage{selinux-cil}
\lstdefinestyle{selinux-cil}{
  language=selinux-cil,
  keywordstyle=\color{blue}\bfseries,        % Declarations
  keywordstyle=[2]\color{teal}\bfseries,     % Transitions
  keywordstyle=[3]\color{violet},            % Logic
  keywordstyle=[4]\color{orange},            % Variables
  keywordstyle=[5]\color{olive}\itshape,     % Labeling/Contexts
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{red},
  breaklines=true,
  showstringspaces=false
}
\usepackage{selinux-policy}
\lstdefinestyle{selinux-policy}{
  language=selinux-policy,
  keywordstyle=\color{blue}\bfseries,        % Declarations
  keywordstyle=[2]\color{teal}\bfseries,     % Transitions
  keywordstyle=[3]\color{violet},            % Logic
  keywordstyle=[4]\color{orange},            % Variables
  keywordstyle=[5]\color{olive}\itshape,     % Labeling/Contexts
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{red},
  breaklines=true,
  showstringspaces=false
}
\usepackage{ima-policy}
\lstdefinestyle{ima-policy}{
  language=ima-policy,
  keywordstyle=\color{blue}\bfseries,        % Actions
  keywordstyle=[2]\color{teal}\bfseries,     % Functions
  keywordstyle=[3]\color{violet},            % LSM
  keywordstyle=[4]\color{brown},             % Keys
  keywordstyle=[5]\color{orange},            % Values/Masks/Templates% Declarations
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{red},
  breaklines=true,
  showstringspaces=false
}

\title{A Framework for Controlled Key Release}

\ifacm
% TODO : orcids
\author{Logan Schmalz}
\email{loganschmalz@ku.edu}
\orcid{1234-5678-9012}
\affiliation{
  \institution{University of Kansas}
  \city{Lawrence}
  \state{Kansas}
  \country{USA}
}

\author{Perry Alexander}
\email{palexand@ku.edu}
\orcid{1234-5678-9012}
\affiliation{
  \institution{University of Kansas}
  \city{Lawrence}
  \state{Kansas}
  \country{USA}}
\fi
\iflncs
  \author{Logan Schmalz \and Perry Alexander}
  \institute{University of Kansas\\
    \email{\{loganschmalz,palexand\}@ku.edu}}
\fi

\newcommand{\tpmsign}{\texttt{tpm\_sign}\xspace}
\newcommand{\wcp}{\texttt{WCP}\xspace}
\newcommand{\pk}{\texttt{SK}\xspace}
\newcommand{\domaint}{\texttt{tpm\_sign\_t}\xspace}
\newcommand{\filet}{\texttt{tpm\_sign\_file\_t}\xspace}
\newcommand{\exect}{\texttt{tpm\_sign\_exec\_t}\xspace}
\newcommand{\pcrpolicy}{\texttt{pcr.policy\_desired}\xspace}
\newcommand{\systemd}{\texttt{systemd}\xspace}
\newcommand{\appraise}{\texttt{appraise}\xspace}
\newcommand{\fix}{\texttt{fix}\xspace}
\newcommand{\initramfs}{\texttt{initramfs}\xspace}

\newcommand{\hash}[1]{\mathop{hash}(#1)}

\begin{document}
\xspaceaddexceptions{\strut}

\ifacm\input{abstract}\fi
\maketitle
\iflncs\input{abstract}\fi

\section{Introduction}
Public key cryptography is a key component of modern security, and is now ubiquitous: most Internet communication is encrypted and authenticated.  % signed ?
However, this security relies on the assumption that private keys are kept secret.
In small-scale, personal settings, users can reasonably trust one another to protect their keys.
In contrast, large-scale systems such as web servers handle thousands of requests per second, and the security of their keys depends entirely on machine configuration and automated processes.
In such settings, keys must be both highly availabe and strongly protected.
% When the data being transmitted is emails, and the correspondents have met in real life,
% it is easy to trust that the other person is conscientious of the security of their key.
% However, when the data being transmitted is a webpage utilizing TLS, from a server that gets thousands of requests per second,
% suddenly the security of the keys depends on the configurations and automated processes of the machines involved.
% TODO: consider reword or cut below
% And when the data being transmitted is evidence that a gamer is not running cheats on their computer,
% letting evidence be controlled by users is detrimental.

A root-level compromise to a system is often catastrophic,
and systems are designed to prevent such events.
Despite best practices, however,
adversaries sometimes gain root capabilities.
This raises a critical question: how can secrets such as private keys be protected even from a root-level attacker, while remaining usable by legitimate services?
% Protecting private keys gainst root-level attackers remains an open-challenge.
% So how do we protect secrets like private keys from root-level attackers?

One approach is to store secrets outside the main memory,
and never expose them to user-space or even kernel-space.
The Trusted Platform Module 2.0 (TPM 2.0) is designed to do exactly this.
It provides a hardware-backed root of trust for storage of private keys and other secrets,
and supports authorization mechanisms such as passwords to access them.
However, these mechanisms do not integrate tightly with operating system concepts such as users or programs.
Authorization can either be satisfied by any program on the machine or requires manual user input.
For high-availability services, manual authorization is infeasible,
and the TPM has no built-in mechanism that meaningfully restricts key usage to a specific user or program.

At the same time, Secure Boot utilizes the TPM to establish firmware and software integrity.
With this, it is possible to ensure that Security-Enhanced Linux (SELinux) and the Linux Integrity Measurement Architecture (IMA)
are enabled with an approved configuration.
Alongside the Extended Verification Module (EVM) and Linux Unified Key Setup (LUKS) containers,
we can enforce fine-grained access control over TPM-resident secrets.

In this work, we demonstrate how to combine these existing security mechanisms with
TPM policy authorization to restrict access to TPM-resident secrets to specific programs or users,
without globally restricting access to the TPM.
We describe the required SELinux, IMA, and EVM configurations, boot-time measurements, LUKS setup, and TPM configuration necessary to achieve this.
Finally, we discuss limitations of current TPM integration and note that deeper kernel support for features such as TPM localities or resettable PCRs could enable stronger future designs.
% The SELinux and IMA policies, the assisting boot-time setup and measurements,
% and the TPM configuration necessary to accomplish this are the key contributions of our work.
% Through these mechanisms, we take a step towards deeper operating system integration with TPM authorization without needing new hardware.
% We also recognize that integrating other TPM mechanisms, such as localities or resettable PCRs, into Linux kernel would allow improved implementations.

\section{Background}
Understanding how these mechanisms work is necessary for our strategy to behave as intended,
as each involves subtleties that significantly affect system security.
% TODO: improve flow

\subsection{Trusted Platform Module (TPM) 2.0}
The TPM 2.0 is widely available on modern x86 systems,
either as firmware TPMs (fTPMs) integrated in AMD and Intel platforms or as discrete hardware modules,
making it a practical target for our work.
% Thus it is a good target for our work, since it is common and provides a portion of the capabilities we desire for protecting keys.
The behavior of the TPM is standardized by the Trusted Computing Group (TCG) \cite{Trusted-Computing-Group:2025:TPM-2.0-Library}.
The TPM serves as a root of trust of storage and root of trust of reporting.
When used for Secure Boot-enabled firmware, it is used to reliably store boot measurements.

\subsubsection{Recording Boot Measurements}
A TPM 2.0 for a PC must have at least 24 Platform Configuration Registers (PCRs) \cite{Trusted-Computing-Group:2023:TCG-PC-Client-PFP}.
PCRs can store hashes of values through hash extension operations.
If a PCR has a current hash value $x$, and it is being extended with a new hash $y$,
the PCR's new value will become $x_{new} = \hash{\mathop{concat}(x,y)}$.
By correlating each hash extension to its source,
successfully reconstructing a PCR value demonstrates integrity of the recorded measurements.
For cryptographically secure hash functions like SHA-256,
it is considered impossible for an adversary to discover $y$ such that $x_{new}$ can lie about previous measurements.

This is the primary mechanism behind Secure Boot and other measured or trusted boot strategies.
When the system boots, its Unified Extensible Firmware Interface (UEFI) measures the system firmware and the bootloader,
and the bootloader measures the kernel and other parts of the operating system.
Because users lack control during boot,
the measurements reliably indicate firmware and software integrity.
The measurements remain trustworthy at runtime as they are stored in PCRs 0--15, which are non-resettable and persist until reboot.

\subsubsection{Protecting Keys and Other Secrets}\label{sec:key-protections}
TPM-resident private keys are only usable within the TPM and will never be available on the operating system.
When they need to be stored elsewhere, i.e. on the filesystem, due to the TPM's limited resources,
they are encrypted so they can only be used if they are reloaded into the same TPM.
Loading a key in the TPM merely caches it; authorization checks are still enforced on use.
The TPM can also store secrets that do get released to the operating system;
these have the same protection mechanisms available.
We choose to focus on TPM-resident keys.

TPM-resident keys support conditional usage,
including requiring a password, HMAC, a particular set of expected PCR values,
or a more general ``policy'' to be satisfied.
Password- or HMAC-based authorization requires a secret to be known at use time,
but such secrets have no inherent protection against disclosure.
Using PCRs effectively limits usage of a key to an administrator-approved boot configuration.
Keys are tied to their authorization mechanism.
For password-authorized keys, it is possible to change the password.
For simple PCR-based authorization,
there is no mechanism to change the expected PCRs,
which causes problems across system updates \cite{Arthur:2015:A-Practical-Guide-to-TPM-2.0-Certifying-Keys,Trusted-Computing-Group:2025:TCG-TPM-2.0-Part1-Architecture}.

Fortunately, policy-based authorization is more advanced.
Flexible policies can be made via ``wildcard policies'' or ``authorized policies'' \cite{Arthur:2015:A-Practical-Guide-to-TPM-2.0-Certifying-Keys}.
Wildcard policies must be signed by an administrator with a private key,
where the corresponding public key is included in the creation details for the TPM-resident key.
Policies can also include PCR checks.
Thus, wildcard policies allow keys to have changeable PCR requirements
and remain usable after system updates that affect boot measurements.
Wildcard policies may also be leveraged to allow multiple policies to authorize usage of a single key,
or to utilize the same policy for different keys, including keys on different devices.

\subsection{Security-Enhanced Linux (SELinux)}
SELinux \cite{Loscocco:2001:SELinux} provides mandatory access controls on Linux and is available by default in most distributions.
Labels added to users, files, and processes determine their permissions according to the SELinux policy installed on the system.
These permissions can control actions such as reading and writing to certain files (including append-only writes),
executing programs, inter-process communi\-cation, and opening specific network ports.
We will use SELinux to protect TPM-related files.

\subsection{Integrity Measurement Architecture (IMA)}
IMA \cite{Sailer:2004:Integrity-Measurement-Architecture,IBM:IMA-Documentation} primarily measures files and prevents them from being opened or executed if they are changed from an approved state.
Like SELinux, IMA is widely available.
When IMA is enabled in \fix mode, all files that get matched by the IMA policy get hashes added as extended file-system attributes.
In \appraise mode, IMA prevents protected files from being opened if their contents do not match the associated hash.
IMA \fix mode can only be enabled when Secure Boot is disabled in the UEFI,
otherwise \appraise mode is forced.
The IMA policy can include various specifications such as file owners, the active user, and whether files are being opened for reading or execution.
The first custom IMA policy that is loaded overwrites any default policies.
Afterwards, the custom IMA policy cannot be overwritten, only appended to.

It is also possible to use signatures instead of hashes to detect corruption of files.
This allows for more dynamicity in the system, as switching IMA from \appraise to \fix mode requires a system reboot, and
allowing hashes to be overwritten at runtime is unsafe, as an adversary may know the expected value.
In contrast, signatures can be updated safely in \appraise mode because the administrator's signing key remains protected.

By default, some distributions require any custom IMA policies and public keys that get loaded to be signed by an administrator.
This involves adding a key certificate to the UEFI Machine Owner Keys, then signing IMA policies and X.509 keys on the filesystem with the appropriate private key.

\subsection{Extended Verification Module (EVM)}
\label{sec:evm}
% TODO: what to cite here? https://lwn.net/Articles/393673/ ima-docs?
EVM is a similar integrity mechanism to IMA, but protects file meta\-data, such as the permissions and SELinux label.
This prevents adversaries from simply adjusting file metadata to bypass SELinux or IMA protections.
EVM can use HMAC to detect any offline filesystem modifications, or use signatures to detect runtime modifications;
both can be enabled at once.
EVM HMACs and signatures are verified when a file matches an IMA appraisal rule.

``Portable'' EVM signatures exclude system-specific metadata and
actively prevent modifying metadata: although IMA prevents \emph{the operation} from occuring \emph{if the file is corrupt},
it does not inherently prevent initial corruption from occuring;
when using EVM portable signatures, any modification to the file's \emph{metadata} will be prevented.
When both HMACs and signatures are enabled, EVM may replace non-portable signatures with HMACs upon metadata changes.
% TODO : consider simplifying this paragraph

\subsection{Linux Unified Key Setup (LUKS) Containers}
LUKS containers are encrypted filesystem images, protected with a password or TPM PCRs.
Thus, LUKS containers can protect files when the system might boot in an unapproved state.
Compared to manual decryption of files, the kernel manages LUKS containers
and files are only decrypted in memory when accessed; % TODO : cite ?
LUKS containers also retain file metadata.
Whether a LUKS container or a manual process is used, a decryption key must be kept in memory. % ? remove this sentence
Thus, we favor the widespread, easy-to-use LUKS mechanisms.

\subsection{Limitations of Available Mechanisms}
As key protections were discussed in \zcref[S]{sec:key-protections},
notice the TPM lacks authorization mechanisms that integrate directly with operating system concepts such as user permissions or programs.
The TPM has a feature called ``localities'' which is intended to be used to indicate the origin of a command (hardware, kernel, user-level software) \cite{Trusted-Computing-Group:2023:TCG-PC-Client-PFP,Arthur:2015:A-Practical-Guide-to-TPM-2.0-Certifying-Keys}.
In theory, using a specific locality to restrict a secret to one program is a good idea.
However, the TPM trusts the operating system to control localities, and there is no existing mechanism, SELinux included, designed to accomplish this.

% ? optional
% While IMA is powerful,
% complex policy configurations can place systems into states that prevent successful boot without reverting to \fix mode.

\section{Framework}
In our framework, access to a TPM key \pk is restricted to a single program, which we denote \tpmsign,
since the key is used for generating signatures.
SELinux enforces this restriction by protecting \tpmsign and a wildcard policy \wcp.
IMA protects the integrity of \tpmsign, as well as any binaries permitted to invoke it.
EVM and SELinux protect the SELinux labels on \tpmsign and \wcp.
%These protections are strongest when the SELinux policy is designated immutable,
%preventing runtime policy modification by an adversary.

The wildcard policy \wcp is the secret used to access \pk.
\wcp requires the PCRs to indicate the system booted with the described mechanisms.
If \wcp is unprotected, the PCRs will indicate such, so \pk will not be usable.
Futhermore, \wcp is stored within a LUKS container which automatically unlocks during boot if the boot process executes as expected.
When the system is booted into an unapproved state, \wcp is encrypted inside the LUKS container and cannot be accessed.
Thus, for an adversary to access \wcp, they must exploit the running system to figure out how to bypass the IMA or SELinux policies,
or otherwise find an exploit to read \wcp from memory.
\zcref[S]{fig:framework} visually illustrates our framework.

\begin{figure}
  \include{trust_diagram}
  \setlength{\abovecaptionskip}{-20pt}
  \caption{An illustration of our framework.
    IMA, SELinux, and EVM protect \tpmsign and \wcp.
    Measurements of those mechanisms are put into TPM PCRs during boot.
    The PCRs must be in the approved state to unlock the LUKS container to access \wcp,
    and also for \tpmsign to use \wcp to access \pk.
  }
  \ifacm
    \Description[Illustration of framework.]{
      The image is an illustration of the components of the framework.
      The components are the TPM, its PCRs, and the signing key;
      the kernel security mechanisms: IMA, SELinux, and EVM;
      the wildcard policy; the LUKS container containing the wildcard policy;
      and the tpm_sign program that can access the wildcard policy.
      Green arrows point from IMA and SELinux to tpm_sign, indicating IMA and SELinux protect tpm_sign.
      Green arrows point from SELinux and EVM to the wildcard policy, indicating SELinux and EVM protect tpm_sign.
      A blue arrow points from all the kernel security mechanisms into the TPM PCRs, indicating the security mechanisms are measured into the PCRs.
      The LUKS container is represented by an orange wrapper around the wildcard policy.
      A blue arrow points from the PCRs to the orange LUKS container, indicating the PCRs are necessary to unlock the LUKS container to access the wildcard policy.
      A dashed blue arrow points from the PCRs to the wildcard policy, indicating the PCRs must still be in an approved state when using the wildcard policy to access the key.
      A double red arrow points from the wildcard policy to the key, going through tpm_sign, indicating that the tpm_sign program uses the wildcard policy as the authorization value to access the key.
    }
  \fi
  \label{fig:framework}
\end{figure}

\subsection{SELinux Policy}
% policy_module(tpm_sign, 1.0.0)
% require {
%         type unconfined_t;
%         role unconfined_r;
%         type kernel_t;
%         type init_t;
%         type initrc_t;
%         type user_home_dir_t;
% }

Our SELinux policy establishes three types:
\domaint for the \tpmsign executing domain, \exect for the \tpmsign executable, and \filet for protected files.
% \begin{minted}{text}
\begin{lstlisting}[style=selinux-policy]
type tpm_sign_t; type tpm_sign_exec_t;
application_domain(tpm_sign_t, tpm_exec_t);
type tpm_sign_file_t; files_type(tpm_sign_file_t);
allow tpm_sign_t tpm_sign_file_t:file { read open create getattr };
\end{lstlisting}
% \end{minted}

We also utilize a short Common Intermediate Language (CIL) policy to enforce explicit deny rules,
which are not available in the standard policy format. % TODO : provide a citation for deny rules
Deny rules are capable of overriding existing policy to prevent otherwise allowed operations.
We prevent even a root user from relabeling a malicious program to \exect, reading or removing protections from \filet files,
or launching or tracing \domaint processes.
% \begin{minted}{text}
\begin{lstlisting}[style=selinux-cil]
(deny domain tpm_sign_exec_t (file (relabelto)))
(deny domain tpm_sign_file_t (file (relabelfrom)))
(deny domain tpm_sign_t (process (ptrace)))
(typeattribute tpm_sign_file_deny)
(typeattributeset tpm_sign_file_deny (not tpm_sign_t))
(deny tpm_sign_file_deny tpm_sign_file_t (file (read)))
(typeattribute tpm_sign_deny)
(typeattributeset tpm_sign_deny (unconfined_t))
(deny tpm_sign_deny tpm_sign_t (process (transition)))
\end{lstlisting}
% \end{minted}
The need for these deny rules is two-fold.
Primarily, they prevent some default allowances made in our policy because we have used macros for simplification,
but the macros are slightly too permissive for our case (see \zcref[S]{appendix:selinux} for further details).
Secondarily, the deny rules serve as a clear and robust way to restrict permissions,
and will prevent accidental allowances due to future policy changes.
Note that the second \verb|typeattributeset| (for the last deny rule) should be made more similar to the first
if only specific programs should be able to launch \tpmsign.

We remove the last deny rule and utilize the following rules in our development environment,
which must be strictly adjusted for real-world systems,
the key adjustments being which types can transition to \domaint.
We allow the \domaint domain to be unconfined since we trust our \tpmsign program,
but further restricting its permissions is reasonable.
% \begin{minted}{text}
\begin{lstlisting}[style=selinux-policy]
type_transition unconfined_t tpm_sign_exec_t:process tpm_sign_t;
allow unconfined_t tpm_sign_t:process transition;
role unconfined_r types { tpm_sign_t tpm_sign_exec_t };
unconfined_domain(tpm_sign_t);
\end{lstlisting}
% \end{minted}
% TODO: allow anyone to delete file?

We recommend making SELinux state immutable using the following command:
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
setsebool -P secure_mode_policyload on
\end{lstlisting}
% \end{minted}
This makes changing policy or changing SELinux to permissive mode impossible at runtime,
and disabling this for approved changes becomes slightly more complex.
If this is not possible, a more complex IMA policy may provide some protection.

Since SELinux policy is often considered frustrating to work with,
we attempted to minimize our reliance on SELinux.
Additionally, since support for deny rules was finalized during our work on this project,
we initially explored alternative mechanisms to further protect our TPM-related files.
Concerned about an adversary replacing the \tpmsign binary with a malicious version or
modifying the relevant SELinux labels, we turned to IMA and EVM.
Further, for our research outside this particular work, we must protect other infrastructure which interacts with
the \tpmsign binary.
While it is likely these concerns could be solved with a strict, immutable SELinux policy and extensive boot measurements,
we believe IMA-based protections are simpler to understand and add an additional, more dynamic layer of protection.

\subsection{IMA Policy}
We need IMA to protect the integrity of \tpmsign binary and relevant files.
Our policy imitates the default \verb|tcb| and \verb|appraise_tcb| policies,
then we include the following lines:
% \begin{minted}{bash}
\begin{lstlisting}[style=ima-policy]
appraise obj_type=tpm_sign_exec_t appraise_type=imasig
appraise obj_type=tpm_sign_files_t appraise_type=imasig
\end{lstlisting}
% \end{minted}
Using \verb|appraise_type=imasig| makes signatures required for these files,
which is stronger than only requiring hashes.
It is also then possible to modify the files without rebooting the system and get an administrator's signature to correct IMA's expected value.
This can make it easier to update relevant files if necessary.
The first rule effectively prevents an adversary from relabeling a malicious binary to \exect,
since all files with that label require an administrator's signature.

The flexibility of IMA policy configuration should make it possible to cover any desired subset of the filesystem
and ensure the desired level of integrity.
Complex policies may be able to provide defense on systems where the SELinux state cannot be immutable
(although this could make the system more difficult to work with than making SELinux state immutable).
At the least, we recommend including the rules of the available default IMA policies. % TODO : cite default rules page

\subsection{Boot-time and Initramfs Measurements}
We add a script to \initramfs that measures the IMA and SELinux policies
as well as the \systemd init binary on the root filesystem during the boot stage.
The script then attempts to unlock the LUKS container which contains \wcp.

% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
#!/bin/sh
INIT="$NEWROOT/usr/bin/init"
SELINUX_POLICY=$(find "$NEWROOT/etc/selinux/targeted/policy" -maxdepth 1 -name 'policy.*' | sort -V | tail -n 1)
IMA_POLICY="$NEWROOT/etc/ima/ima-policy"

INIT_SHA=$(sha256sum "$INIT" | cut -d' ' -f1)
tpm2_pcrextend 11:sha256="$INIT_SHA"
# ... similar for SELinux, IMA
HASH=$(printf 'done' | sha256sum | cut -d' ' -f1)
tpm2_pcrextend 11:sha256="$HASH"

IMG_PATH="$NEWROOT/home/user/vault.img"
MAPPER_NAME='vault_mapper'
MOUNT_POINT="$NEWROOT/mnt/vault"

mokutil --sb-state | grep -q 'enabled' || exit 0
test -e "$IMG_PATH" || exit 0
systemd-cryptsetup attach "$MAPPER_NAME" "$IMG_PATH" - tpm2-device=auto,keyfile-timeout=0,tries=1,nofail || exit 0
udevadm settle -E "/dev/mapper/$MAPPER_NAME" -t 2 || exit 0

mkdir -p "$MOUNT_POINT"
mount "/dev/mapper/$MAPPER_NAME" "$MOUNT_POINT" 
\end{lstlisting}

Since this script is installed in \initramfs,
removal of the script or other unapproved modifications of \initramfs will be reflected in a PCR.
Thus, it is not possible for an adversary to falsify the measurements made by this script. % ! (or its absence).

% ? \subsubsection{Relevant TPM PCRs}
We store our measurements in PCR 11.
Other relevant PCRs are 4, 7, 8, 9, and 14 \cite{UAPILinuxTPMPCR}.
The UEFI measures the GRUB bootloader into PCR 4.
GRUB measures the command line arguments into PCR 8,
and \initramfs and the kernel into PCR 9 \cite{gnu_grub_measured_boot,gentoo_wiki_secure_boot_grub}.
PCR 7 contains the general Secure Boot state, including the keys used to verify bootup.
PCR 14 contains measurements of Machine Owner Keys, including those necessary for loading custom IMA policies.
These measurements are reflected in \zcref[S]{tab:pcr-usage}.

\begin{table}
  \caption{PCR usage at start-up}
  \label{tab:pcr-usage}
  % \footnotesize
  \begin{tabular}{ccl}
    \toprule
    Actor  & PCR & Value                                       \\
    \midrule
    UEFI   & 4   & $\hash{\mbox{shim}}$$^\dagger$              \\
    UEFI   & 7   & $\hash{\mbox{{Secure Boot configuration}}}$ \\
    shim   & 4   & $\hash{\mbox{GRUB}}$                        \\
    shim   & 7   & $\hash{\mbox{shim certificate database}}$   \\
      \addlinespace
    GRUB   & 8   & $\hash{\mbox{kernel command line args}}$    \\
    GRUB   & 9   & $\hash{\mbox{\initramfs}}$                  \\
    GRUB   & 9   & $\hash{\mbox{kernel}}$                      \\
      \addlinespace
    Script & 11  & $\hash{\mbox{\systemd}}$                    \\
    Script & 11  & $\hash{\mbox{SELinux policy}}$              \\
    Script & 11  & $\hash{\mbox{IMA policy}}$                  \\
    Script & 11  & $\hash{\mbox{\texttt{`done'}}}$             \\
    \bottomrule
  \end{tabular}
  \\
  $^\dagger$ See \zcref[S]{sec:implementation-details}.
\end{table}

\subsection{TPM Key Setup}
After rebooting the system with our \initramfs changes,
we set up \wcp and \pk.

\subsubsection{Policy Creation}
For our development system, we have the adminstrator's private key available---in a real setup, it should be held on a different machine.
We create the administrator keys with OpenSSL.
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
openssl genrsa -out policy_key_private.pem 2048
openssl rsa -in policy_key_private.pem -out policy_key.pem -pubout
\end{lstlisting}
% \end{minted}

The public portion of the key must be loaded in the TPM that holds \pk.
We create a ``wildcard policy digest'' so \pk can require a wildcard policy authenticated by the administrator's public key.
The important file being created here is \verb|authorized.policy|.
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
tpm2_loadexternal -G rsa -C o -u policy_key.pem -c policy_key.ctx -n policy_key.name
tpm2_startauthsession -S session.ctx
tpm2_policyauthorize -S session.ctx -L authorized.policy -n policy_key.name
tpm2_flushcontext session.ctx
\end{lstlisting}
% \end{minted}

The wildcard policy that sets the PCR requirements must be created and signed with the private key created earlier.
The file \pcrpolicy is \wcp.
Our commands create the policy on our development machine,
but that is primarily to easily collect the PCR values.
It should be possible to create the policy on another machine when the PCR values are known.
To update \wcp (e.g. after system updates), this is the step that must be repeated.
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
tpm2_pcrread -opcrs.sha256 sha256:7,8,9,11,14
tpm2_startauthsession -S session.ctx
tpm2_policypcr -S session.ctx -l sha256:7,8,9,11,14 -f pcrs.sha256 -L pcr.policy_desired
tpm2_flushcontext session.ctx
openssl dgst -sha256 -sign policy_key_private.pem -out pcr.signature pcr.policy_desired
\end{lstlisting}
% \end{minted}

\subsubsection{Key Creation}
First we create a primary key. Primary keys are deterministically generated by the TPM,
and the same one will be recreated by re-running the following command:
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
tpm2_createprimary -C o -g sha256 -G aes128cfb -c prim.ctx
\end{lstlisting}
% \end{minted}
Then we create \pk, whose parent key is the primary key.
Non-primary keys are randomly generated,
so having a primary key parent allows \pk to be saved and reloaded.
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
tpm2_create -u key.pub -r key.priv -g sha256 -G 'rsa2048:rsapss:null' -a 'sign|fixedtpm|fixedparent|sensitivedataorigin' -C prim.ctx -c signing.ctx -L policy/authorized.policy
tpm2_readpublic -c signing.ctx -o key.pem --format=pem
rm prim.ctx signing.ctx
\end{lstlisting}
% \end{minted}
The important files here are \verb|key.pub| and \verb|key.priv|, the public and private portions of \pk.
We also create a PEM-formatted copy of the public key for sharing.

\subsubsection{Using the Signing Key}
Our program \tpmsign is the only program allowed to access
\wcp (\pcrpolicy) by labeling it \exect.
It attempts to reload \pk from a transient session,
and if no valid session exists, it re-generates the primary key and loads \pk from its file.
\pk and the administrator's public policy key may optionally be placed in the TPM's non-volatile memory to reduce loading overhead.
After loading, every use of \pk must follow the standard protocol to authorize key usage with the wildcard policy \cite{tpm2-software:2026:policyauthorize}.

\subsection{Using EVM on \wcp}
After giving \wcp the \filet label,
we use a portable EVM signature to protect it.
We apply the EVM portable signature (and also an IMA signature) to \wcp via \verb|evmctl| \cite{IBM:IMA-utilities-evmctl}.
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
evmctl sign --imasig --portable pcr.policy_desired -k key.priv
\end{lstlisting}
% \end{minted}
This makes it impossible to change the SELinux labels, file owners, or UNIX permissions of \wcp.
In a real environment, the private key should be held on a separate machine
and the signature should be assigned manually to the file via \verb|setfattr|.



\subsection{LUKS Container}
% TODO : maybe cite something here?
We set up a LUKS container to hold \wcp via the following steps:
% \begin{minted}{bash}
\begin{lstlisting}[style=bash]
fallocate -l 1GB vault.img
cryptsetup -v luksFormat vault.img
losetup --find --show vault.img
systemd-cryptenroll --wipe-slot=tpm2 --tpm2-device=auto --tpm2-pcrs=4+7+8+9+11 /dev/loop0
losetup -d /dev/loop0
cryptsetup -v luksOpen vault.img vault_mapper
mkfs -t ext4 /dev/mapper/vault_mapper
mkdir -p /mnt/vault/
mount /dev/mapper/vault_mapper /mnt/vault
\end{lstlisting}
% \end{minted}
Then \wcp must be placed into the container mount.

\subsection{Deployment Considerations}\label{sec:implementation-details}
Our development system uses Fedora 42 with an out-of-the-box kernel.
This influenced some of our design decisions which may need to be modified for other distributions.

Firstly, many distributions use a Microsoft-signed boot shim to facilitate Secure Boot support \cite{rhboot:2026:shim}.
The shim contains its own certificate database to check signatures, so
the UEFI measures the shim, and the shim measures the real bootloader (e.g. GRUB).
This does not affect the security of our framework, as the chain of trust remains intact.
Alternatively, administrators may manually sign the bootloader or use a unified kernel image (UKI) \cite{gentoo_wiki_secure_boot,gentoo_wiki_secure_boot_grub}.

Further, while PCR 11 is unused and available on Fedora when using the GRUB bootloader, only the usage of PCRs 0--7 is defined by TCG standards.
Usage of PCRs 8--15 may vary for other boot setups \cite{Trusted-Computing-Group:2023:TCG-PC-Client-PFP,UAPILinuxTPMPCR}.

We found that attempting to run EVM without HMAC support prevents Fedora from booting due to its reliance on some dynamically-modified files.
As mentioned in \zcref[S]{sec:evm}, this means EVM may replace non-portable signatures with HMACs, allowing metadata to be modified.
In this case, \textbf{portable} signatures are absolutely necessary.

Finally, on some distributions it should be possible to load the SELinux and IMA policies from the measurement script.
However, Fedora's built-in SELinux policy is not designed to support loading before \verb|switch_root|,
and IMA policies containing SELinux labels will fail to load if the SELinux policy is not loaded beforehand.
To ensure the policies are loaded, we measure the \systemd init binary as it is responsible for loading the policies after \verb|switch_root|.
When the measurement script loads the policies, or when the initial IMA policies
measure \systemd (i.e. \verb|appraise_tcb|), this is optional.


\section{Evaluation}
We experimentally demonstrated the following features of this setup:
\begin{itemize}
  \item Changing the \initramfs, kernel, IMA policy, SELinux policy, and other items measured into PCRs results in an unapproved boot state.
  \item When the system is booted in an unapproved state:
        \begin{itemize}
          \item The LUKS container will not open without a password, and \wcp will not be available.
                Thus, \wcp cannot be relabelled either.
          \item If somehow the container is unlocked and \wcp is available, the PCRs specified in \wcp will not match and \pk will not be usable.
                Thus, the contents of \wcp must be maintained until the system is rebooted to an approved state.
          \item It is not possible to corrupt the IMA policy, because IMA policies require a signature.
        \end{itemize}
  \item When the system is booted in an approved state (IMA and SELinux working as intended), it is not possible to:
        \begin{itemize}
          \item Give unapproved programs the \exect label
          \item Corrupt the \tpmsign binary to leak information
          \item Remove the \filet label from \wcp
          \item Corrupt the IMA or SELinux policies
          \item Access \wcp without \tpmsign
        \end{itemize}
\end{itemize}

\newcommand{\yes}{\textcolor{green}{\ding{52}}} % Checkmark
\newcommand{\no}{\textcolor{red}{\ding{56}}}   % X mark
\newcommand{\na}{\textcolor{gray}{N.A.}}   % Minus mark

% \begin{comment}
% \begin{table*}[phtb]
%   \begin{tabular}{c c c c c c}
%     \toprule
%     \textbf{Boot State} & \textbf{\wcp Available} & \textbf{\wcp Readable} & \textbf{Modify \wcp} & \textbf{Modify IMA Policy} & \textbf{Modify SELinux Policy} \\
%     \midrule
%     Unapproved          & \no                     & If made available      & If made available    & \no                        & \yes                           \\
%     \addlinespace
%     Approved            & \yes                    & By \tpmsign            & \no                  & \no                        & \no                            \\
%     \bottomrule
%   \end{tabular}
% \end{table*}
% \end{comment}


Our framework does not defend against attacks that can leak arbitrary program memory, like side-channel attacks.
Our \tpmsign binary is short-lived to minimize the surface area for these attacks.
We can also demonstrate a rootkit that corrupts IMA and/or SELinux behavior, bypassing our protections.
However, on a well-protected system, especially when IMA checks kernel modules, it is very difficult to actually install such rootkits.
Thus, we believe bypassing our framework on an otherwise uncorrupted, up-to-date kernel has equivalent difficulty to finding at least one zero-day exploit.

Even though the LUKS container strategy should protect \wcp (or a password) sufficiently,
\wcp including PCRs related to our protection mechanisms provides some extra assurance that if \pk is used, then the system at least booted in a state approved by \wcp.
For example, since it is sometimes necessary to unlock the LUKS container when the system is in an unapproved state to accomodate a system update, it is still beneficial to know \pk cannot be used at that moment.

\section{Limitations}
There are three primary limitations of this secret protection scheme.
The first is in real-world scenarios, strong IMA policies (protecting infrastructure other than ours) can make systems annoying to change or update.
This is a recognized problem and work is being done to mitigate this---for example,
by making restrictions on approved kernel modules more practical.
%---but such improvements are not yet widely deployed.
% TODO: cite

The second limitation is assuming IMA and SELinux protections are not bypassed under an approved boot.
Against an adversary capable of bypassing SELinux and IMA, installing a rootkit, or otherwise corrupting the kernel at runtime,
the contents of \wcp can be leaked such that it, and thus \pk, will be persistently available to the adversary until boot PCRs change (e.g. via a system update),
regardless of the state of SELinux and IMA.
% ? Attacks in the vein of rootkits disabling SELinux/IMA functionality are demonstrable.

The final limitation is the speed of the TPM.
A non-virtualized TPM typically takes a few hundred milliseconds to complete the sequence of operations required for signing.
Thus, the TPM is a limiting factor for environments that would need high-frequency operations.
% ? The slow speed of the TPM is considered a design feature to limit attack surface area.
Alternative hardware security modules or virtualized TPMs may improve on this.

\section{Variations and Future Work}
The design of our framework allows for several adjustments to address specific concerns.

We considered two relatively straightforward modifications to our implementation.
First, using a unified kernel image could reduce boot and measurement complexity.
Second, writing a minimal, bespoke kernel module to protect \wcp would avoid the need for intricate SELinux and IMA policies and the associated administrative overhead.
Each change would reduce the trusted computing base by reducing the amount of configuration that needs to be verified.

If there is serious concern about authenticity across boot cycles, two additional key management strategies are feasible.
Policies can require a check to the boot-count, so \wcp could be reprovisioned on every boot.
Alternatively, \pk could be reprovisioned on every boot, but this would require remote parties to verify the PCRs to ensure \pk is properly protected
(and TPM-resident, if that is still desired).
These options restrict runtime leaks to a single boot cycle, at the cost of increased complexity in policy or key management respectively.

While other hardware security modules with similar functionality to the TPM exist, they are not widely available.
Trusted execution environments (TEEs) are, however, commonly available on enterprise hardware,
and provide strong memory-based protections.
TEEs may provide sufficient isolation to justify storing \wcp or \pk directly in protected memory,
potentially improving performance.

Implementing support for TPM localities on Linux and SELinux (or equivalent) would be a significant step toward hardening our framework.
By restricting access to TPM localities to certain programs,
\pk could require a locality accessible only by \tpmsign, making \pk's use independent of the secrecy of \wcp.
However, adversaries capable of corrupting the kernel/SELinux would still be able to work around this.
This is our ideal way forward.




%
% 
% ---- Bibliography ----
%
% BibTeX users should specify bibliography style 'splncs04'.
% References will then be sorted and formatted in the correct style.
%
\ifacm\bibliographystyle{ACM-Reference-Format}\fi
\iflncs\bibliographystyle{splncs04}\fi
\bibliography{ref}

\appendix
\section{Details of SELinux Policy}\label{appendix:selinux}
There are multiple variants on the SELinux policy setup to accomplish the same protections as in the main portion of the text.
Here, we discuss some of the nuances for various choices.
Notably, in almost all of the basic SELinux policy tutorials, these details are not included.
This is largely because most SELinux policies for applications do not need to be as strict as ours, and instead simply need to follow a relatively standard template.

The first important detail to note is the existence of the SELinux Reference Policy. % TODO : cite
The Reference Policy serves as the basis for most out-of-the-box SELinux policies and includes some useful macros which are widely used.
One particular macro is \verb|files_type(x)|, which nominally associates the type \verb|x| with the type \verb|file_type|.
The \verb|file_type| type includes permissions allowing unconfined contexts to access and manipulate files with label \verb|x|.
This is entirely reasonable in most situations where an administrator should be easily allowed to manipulate files.
However, it is not suitable for developing protections such as ours,
as unconfined contexts are given the following permissions:
\begin{lstlisting}[style=selinux-policy]
allow files_unconfined_type file_type:file { exec_file_perms manage_file_perms mounton quotaon relabel_file_perms watch watch_mount watch_reads watch_sb watch_with_perm };
\end{lstlisting}
% TODO: cite https://github.com/SELinuxProject/refpolicy/blob/2cba8023863718709d0349faf62a9f4da2248a3f/policy/modules/kernel/files.te#L230,
% TODO: cite https://github.com/SELinuxProject/refpolicy/blob/2cba8023863718709d0349faf62a9f4da2248a3f/policy/support/obj_perm_sets.spt#L158
We found ourselves concerned with
\verb|exec_file_perms|, which is a macro that includes \verb|open| and \verb|read|.
and with \verb|relabel_file_perms|, which includes \verb|relabelfrom| and \verb|relabelto|.
In Fedora's \verb|targeted| policy specifically, unconfined contexts are also able to transition to any domain type and inspect the memory of any program. % TODO: improve
By parsing through these definitions in the Reference Policy, it is possible to recreate them while excluding undesirable permissions.

The second important detail to note is the implementation of deny rules in the SELinux Common Intermediate Language.
Deny rules are capable of overriding existing policy, however they are not available in the high-level SELinux syntax.
Since deny rules are very explicit, they can compensate for pre-existing SELinux policy modules which leave unintended security holes.
Thus, they are a suitable alternative to the above process of manually recreating simple things which could be based on existing macros.
For example, to recreate our policy while making denials explicit:
\begin{lstlisting}[style=selinux-cil]
(deny domain tpm_sign_exec_t (file (relabelto)))
(deny domain tpm_sign_file_t (file (relabelfrom)))

(typeattribute tpm_sign_file_deny)
(typeattributeset tpm_sign_file_deny (not tpm_sign_t))
(deny tpm_sign_file_deny tpm_sign_file_t (file (read)))
\end{lstlisting}
The \verb|typeattribute|/\verb|typeattributeset| declaration is necessary to determine which types should be denied in the last deny rule
via logical expressions like \verb|not|, \verb|and|, \verb|or|, etc.
This is because, in CIL policy, rules (\verb|allow|, \verb|deny|, etc.) must take an identifier in the first and second arguments.
In our policy, this allows us to deny reads from every domain except \domaint.

\section{Setting up IMA}
Setting up IMA is a complicated process on Fedora.
To ensure trustworthiness of the installed IMA policy and signatures, there are some setup steps that go beyond ``put a file in a place''.
We recommend at least reading the sections starting from ``IMA CA Key and Certificate'' to ``Sign and Install a Custom Policy'' at \cite{IBM:IMA-utilities-evmctl} to gather the basics.

On Fedora, dracut is the tool used to build \initramfs.
Dracut has a standard module for loading IMA/EVM keys and enabling IMA/EVM during boot named \verb|integrity|.
It is necessary to remove the \verb|ima-policy-load.sh| script from this module, since Fedora's SELinux policy is not designed to load during boot and our IMA policy relies on the SELinux policy.
The \verb|integrity| module loads IMA keys from \verb|/etc/keys/ima/| as described in \cite{IBM:IMA-utilities-evmctl}.
Before that, it loads the EVM keys from \verb|/etc/keys/evm/|;
thus, another key with a similar setup (or the same key as generated for IMA in the ``IMA Signing Key and Certificate'' section) must also be placed there to serve as the EVM signature verification key.

As discussed briefly in \zcref{sec:implementation-details}, it seems best on Fedora to enable EVM HMACs as well.
This requires creating a kernel masterkey (we make a trusted masterkey since we are using a TPM) and making a child encrypted EVM HMAC key.
\begin{lstlisting}[style=bash]
tpm2_createprimary --key-algorithm=rsa2048 --key-context=key.ctxt
tpm2_evictcontrol -c key.ctxt 0x81000001
KMK=$(keyctl add trusted kmk-trusted "new 32 keyhandle=0x81000001" @u)
keyctl pipe $KMK > /etc/keys/kmk-trusted.blob
EVM=$(keyctl add encrypted evm-key "new trusted:kmk-trusted 64" @u)
keyctl pipe $EVM > /etc/keys/evm-trusted.blob
\end{lstlisting}
% TODO : cite? https://web.archive.org/web/20250709141759/https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/managing_monitoring_and_updating_the_kernel/enhancing-security-with-the-kernel-integrity-subsystem_managing-monitoring-and-updating-the-kernel#working-with-trusted-keys_enhancing-security-with-the-kernel-integrity-subsystem


\end{document}
